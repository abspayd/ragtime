services:
  qdrant:
    image: qdrant/qdrant:latest
    restart: always
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_data:/qdrant/storage

  # app:
  #   build: .
  #   environment:
  #     - QDRANT_HOST=qdrant
  #   depends_on:
  #     - qdrant

  #   develop:
  #     watch:
  #       - action: sync
  #         path: .
  #         target: /app
  #         ignore:
  #           - .venv

  #       - action: rebuild
  #         path: ./pyproject.toml
